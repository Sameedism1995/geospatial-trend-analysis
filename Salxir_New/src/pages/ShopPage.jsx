import { PlaceholderPage } from '../components/PlaceholderPage';

export function ShopPage() {
  return (
    <PlaceholderPage
      eyebrow="Shop"
      title="Consumer product catalog"
      description="A minimal shop route prepared for product detail pages, checkout flows, and richer merchandising in the next iteration."
      cta={{ href: '/', label: 'Back to Home' }}
    />
  );
}
