## Figure 5.10a — Weekly co-occurrence (pairwise heatmap)

Off-diagonal and symmetric entries count **how many week-starts** both indicators simultaneously exceed the **rolling nine-week baseline** residual pooled at the **93rd percentile** (see `anomaly_detection_summary.csv`). Diagonal: total anomaly weeks for that indicator.
